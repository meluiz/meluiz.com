export { countries } from './countries';
export {
  ldJSON,
  manifest,
  metadata,
  robots,
  sitemap,
  viewport,
} from './layout';
export { about, avatar } from './profile';
export { buildInfo, projects, stacks } from './project';
export { github } from './socials';
